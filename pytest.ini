[pytest]
testpaths = src/tests
pythonpath = src
addopts = -ra
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    browser: testes E2E que exigem navegador real via Playwright
    integration: testes de integração Flask + Playwright com mocks
