[pytest]
testpaths = src/tests
pythonpath = src
addopts = -ra
markers =
    browser: testes E2E que exigem navegador real via pytest-playwright
    integration: testes de integração Flask + Playwright com mocks
